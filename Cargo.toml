[package]
name = "robson"
version = "0.0.7"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[profile.release]
lto = "fat"
panic = "abort"

[dependencies]
crossterm = "0.23.2"
robson_compiler = { path = "./robson_compiler" }
